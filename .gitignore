# environments and caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.coverage
.coverage.*

# pipeline data: everything under data/ except the committed ledgers
data/raw/
data/work/
data/staged/
data/fixture_run/

# model outputs: checkpoints, scored runs, analyses and figures
runs/
results/
figures/

# bulk astronomical files anywhere, except the committed test fixtures
*.fits
*.fits.gz
*.h5
*.hdf5
*.npz
*.parquet
!tests/fixtures/**/*.fits
!tests/fixtures/**/*.npz

# local scratch
*.log
nohup.out
*.pdf

# the working plans stay with the maintainers; docs/DATA.md is the released contract
docs/PLAN.md
docs/PLAN_PHASE2.md

# port notes from the old code base: maintainers only, like the plan
docs/port_notes/
